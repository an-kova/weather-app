# Weather App

Basic Weather Forecast App in Vanilla JS

## Project Setup

```shell
npm install
```

For development build and dev server:

```shell
npm run start
```

For production build:

```shell
npm run build
```
